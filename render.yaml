services:
  - type: web
    name: peaceplus-ai-backend
    env: python
    rootDir: forgiveness_backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
